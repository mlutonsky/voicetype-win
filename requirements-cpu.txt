# CPU-only variant (no NVIDIA GPU). Use instead of requirements.txt and set device = "cpu" in config.toml.
onnxruntime           # CPU runtime (NOT onnxruntime-gpu)
onnx-asr[hub]
sounddevice
keyboard
pyperclip
numpy
pystray
Pillow
winsdk
